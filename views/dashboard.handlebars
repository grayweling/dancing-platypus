<body class="bg-cyan-200">
    <nav class="p-5 bg-white shadow md:flex md:items-center md:justify-between">
        <div class="flex justify-between items-center">
            <span class="tex -2xl cursor-pointer"> 
                <img class="h-10 inline" src= images/platypus.jpeg>
                PAGE NAME
            </span>
            <span class="text-3xl cursor-pointer mx-2 md:hidden block">
                <ion-icon name="menu" onclick="Menu(this)"></ion-icon>
            </span>
        </div>
        <ul class="md:flex md:items-center z-[-1] md:z-auto md:static absolute bg-white w-full left-0 md:w-auto md:py-0 py-4 md:pl-0 pl-7 md:opacity-100 opacity-0 top-[-400px] transition-all ease-in duration-500">
            <li class="mx-4 my-6 md:my-0"> 
                <a href="#" class="text-xl hover:text-cyan-300 duration-500">HIGH SCORES</a>
            </li>
            <li class="mx-4 my-6 md:my-0">
                <a href="#" class="text-xl hover:text-cyan-300 duration-500">BLOG</a>
            </li>
            <ul>
                <button class="bg-cyan-400 cursor-pointer text-white rounded-md   duration-500 px-6 py-2 mx-4 hover:bg-slate-500 hover:outline outline-2 outline-black outline-offset-2 text-sm">
                Sign Out
            </ul>
        </ul>

    </nav>

    <div>
        <h3>Click Me</h3>
        <button id="clickme">Click me: 0 
            <class:body></class:body></button>        
    </div>
    
    <script>
        function Menu(e){
            let list = document.querySelector('ul');

            e.name === 'menu' ? (e.name = "close",list.classList.add('top-[80px]') , list.classList.add('opacity-100')) :( e.name ="menu" ,list.classList.remove('top-[80px]'),list.classList.remove(opacity-100))
        }
    </script>
    <script>
         var button = document.getElementById("clickme"),
        count = 0;
        button.onclick = function() {
        count += 1;
        button.innerHTML = "Click me: " + count;
  };
    </script>
